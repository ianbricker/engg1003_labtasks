//Write a C function which takes a single int * arg and zeroes it
/*#include <stdio.h>

void zeroInt(int *x);

int main() 
{
    int *x, k = 3;
    x = &k;
    printf("k: %d\n", k);
    zeroInt(&x);
    printf("return: %d", x);
    
}

void zeroInt(int *x) 
{
    *x = 0;
} */